Часовой пояс
===

Часовой пояс отправляется в виде meta-параметра `timeZone`.

Например:

```json
{
  "jsonrpc": "2.0",
  "method": "my-contact.all",
  "params": {
    "meta": {
      "timeZone": "Europe/Berlin"
    }
  }
}
```
